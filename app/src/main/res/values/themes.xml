<resources>
    <!-- Base application theme. -->
    <style name="Theme.CustomViews" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="materialButtonStyle">@style/myButtonStyle</item>
    </style>

    <style name="myButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="negativeButton" parent="myButtonStyle">
<!--        <item name="backgroundTint">@color/black</item>-->
        <item name="rippleColor">@color/red_highlight</item>
    </style>

    <style name="positiveButton" parent="myButtonStyle">
        <item name="backgroundTint">@color/green_200</item>
        <item name="rippleColor">@color/green_highlight</item>
    </style>

</resources>